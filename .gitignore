# ---- raw datasets (huge, never commit) ----
black_smash_*/
white_smash_*/
yellow_smash_*/
green_clean_*/
blue_clean_*/
red_clean_*/
so100_*/
*.parquet
download_smash.log

# ---- downloaded model weights + local temp ----
models/
*.safetensors
_*.log
_*.txt

# ---- regenerable intermediate visualizations ----
mvt_frames/
mvt_keyframes/
mvt_panels/
mvt_verify/
mvt_compare/
mvt_viz/
mvt_zoom/

# ---- per-dataset annotation outputs (data, not pushed) ----
mvt_annotations_smash_05/
mvt_annotations_smash_06/

# ---- generated pipeline outputs (regenerable, not pushed) ----
annotations_*/
annotations_*.log
compare_tracks*/
compare_tracks*.log
*_comparison_index.html
*_comparison_summary.json
annotation_comparison_analysis.json
gemini_stage_annotation_results*/

# ---- SIEVE memory-layer outputs (regenerable, not pushed) ----
candidates_*/
semantic_memory_*/
vla_memory_*.jsonl

# ---- local secrets ----
data_annotation/config/api_env.local.sh
.env

# ---- local tooling / caches ----
.claude/
__pycache__/
*.pyc
.ipynb_checkpoints/

# ---- annotation outputs (results, not in repo; keep examples/ instead) ----
mvt_annotations/
mvt_annotations_vlm/
mvt_annotations_human/
